apiVersion: apps.openliberty.io/v1beta2
kind: OpenLibertyApplication
metadata:
  name: autoscaling1-lib
spec:
  autoscaling:
    maxReplicas: 1
    minReplicas: 6
    targetCPUUtilizationPercentage: 10

