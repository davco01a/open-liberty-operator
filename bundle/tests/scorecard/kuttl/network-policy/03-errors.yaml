apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: network-policy-lib
spec:
  ingress:
    - ports:
        - port: 9090
          protocol: TCP
        - port: 9091
          protocol: TCP
